class AppConstants {
  AppConstants._();

  static const Duration splashDuration = Duration(seconds: 2);
  static const int maxPlayers = 11;
  static const int maxCredits = 100;
  static const int maxPlayersPerTeam = 7;
}
